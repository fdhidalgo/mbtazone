% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_parcel_config.R
\name{parcel_sampler_spec_default}
\alias{parcel_sampler_spec_default}
\title{Default parcel sampler spec preset}
\usage{
parcel_sampler_spec_default(
  n_steps,
  swap_cap_tolerance,
  birth_tilt_lambda,
  debug_invariant_checks = FALSE,
  ...
)
}
\arguments{
\item{n_steps}{Number of MCMC iterations}

\item{swap_cap_tolerance}{Capacity tolerance for secondary-swap sampling}

\item{birth_tilt_lambda}{Tilt strength for capacity-weighted birth proposals}

\item{debug_invariant_checks}{Validate state invariants after every
accepted move}

\item{...}{Additional fields forwarded to \code{parcel_sampler_spec()}
(\code{mcmc_burn_in}, \code{enable_online_enrichment}, \code{enrichment_interval},
\code{max_online_entries}, \code{enrichment_burn_in}, \code{max_stored_samples},
\code{store_lcc_signatures}) — see there for defaults}
}
\value{
Named list sampler spec
}
\description{
4 active kernels with capacity prior. Capacity prior penalizes capacity
above min_capacity (soft preference, not rejection). Hard constraints
(min_capacity, min_area, min_density, min_lcc_fraction) live in
\code{target_spec}, not here. min_lcc_fraction (0.5) is a hard legal
constraint, so LCC capacity must be sufficient before symmetric_birth_death
can add secondaries without violation.
}
\details{
Note: symmetric_birth_death replaces separate birth/death kernels to fix
the proposal asymmetry that caused 5.4\% death acceptance (k/N ratio).
}

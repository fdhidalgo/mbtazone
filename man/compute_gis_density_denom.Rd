% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_soft_constraints.R
\name{compute_gis_density_denom}
\alias{compute_gis_density_denom}
\title{GIS-based density denominator for a set of MCMC units}
\usage{
compute_gis_density_denom(unit_ids, constraints)
}
\arguments{
\item{unit_ids}{Character vector of unit IDs (from parcel graph vertices)}

\item{constraints}{Constraints list from \code{\link[=define_constraints]{define_constraints()}}}
}
\value{
Numeric scalar: density denominator in acres (0 when the union lies
entirely inside the deductions)
}
\description{
Unions the parcel geometries for the given unit IDs, applies a morphological
close (buffer out then back by \code{constraints$row_fill_m}) to fill road
right-of-way gaps between adjacent parcels, then subtracts the pre-dissolved
local density deductions to get the district area in acres.
}
\details{
When \code{constraints$row_sfc} is set (by passing \code{right_of_way_sf} to
\code{\link[=define_constraints]{define_constraints()}}), the fill is ROW-constrained: only the portion of
the morphological close that overlaps the right-of-way polygon is added to
the parcel union.

Without \code{row_sfc}, falls back to an unconstrained morphological close that
fills all gaps narrower than \code{2 * row_fill_m} (e.g. 18.29 m fills gaps up
to 36.58 m ≈ 120 ft). Set \code{row_fill_m = 0} in \code{\link[=define_constraints]{define_constraints()}} to
disable filling entirely.

Callers apply this ONE BLOCK AT A TIME: an MCMC state's denominator is the
closed area of its LCC block plus the closed area of each secondary block,
each closed independently. Road fill between two different blocks is excluded
by definition, which makes block areas exactly additive.
}
\keyword{internal}

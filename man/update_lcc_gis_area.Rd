% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_parcel_mcmc_state.R
\name{update_lcc_gis_area}
\alias{update_lcc_gis_area}
\title{Refresh the cached GIS denominator after an LCC-local move}
\usage{
update_lcc_gis_area(new_state, old_state, constraints)
}
\arguments{
\item{new_state}{State returned by \code{\link{update_lcc}}.}

\item{old_state}{State the move started from; must carry both cached fields.}

\item{constraints}{Constraints list from \code{\link{define_constraints}}.}
}
\value{
\code{new_state} with \code{lcc_gis_area} and \code{total_gis_area} set.
}
\description{
The denominator is per block by definition: the LCC block closed on its own
plus each secondary block closed on its own, with cross-block road fill
excluded. An LCC-local move changes only the LCC, so the secondary
contribution carries over as (old total - old LCC area) and only the new LCC
block is unioned — never the whole state.
}
\keyword{internal}

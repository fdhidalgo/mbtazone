% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_parcel_library.R
\name{generate_k0_initial_states}
\alias{generate_k0_initial_states}
\title{Generate k=0 (no-secondary) initial states from standalone-feasible LCCs}
\usage{
generate_k0_initial_states(
  lcc_library,
  secondary_library,
  parcel_graph,
  constraints,
  n_k0_chains = 2L
)
}
\arguments{
\item{lcc_library}{LCC library (will be hydrated internally)}

\item{secondary_library}{Secondary block library}

\item{parcel_graph}{igraph object with parcel attributes}

\item{constraints}{MBTA constraints list}

\item{n_k0_chains}{Number of k=0 chains to produce (default 2)}
}
\value{
List of initialised parcel MCMC states (length 0 to \code{n_k0_chains}).
Returns empty list with a warning if no standalone-feasible LCCs exist.
}
\description{
Produces initial MCMC states containing only an LCC component (zero secondary
blocks). These states let some chains explore the low-k region of the
posterior, which is hard to reach from k>0 due to birth/death pool asymmetry.
}
\details{
LCCs that satisfy all hard constraints on their own (capacity, area, density,
station proximity, connectivity) are identified via \code{find_feasible_lcc_ids()}.
The most geographically distant candidates are selected as seeds.
}

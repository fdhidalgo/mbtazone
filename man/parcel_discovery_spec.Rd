% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_config.R
\name{parcel_discovery_spec}
\alias{parcel_discovery_spec}
\title{Define the parcel MCMC discovery spec}
\usage{
parcel_discovery_spec(
  discovery_capacity_multiplier = 2.5,
  tree_lcc_n_trees = 500L,
  bfs_lcc_n_samples = 100L,
  bfs_lcc_n_seeds = 10L,
  lcc_capacity_bands_relative = list(c(0.5, 0.75), c(0.75, 1), c(1, 1.25), c(1.25, 1.5),
    c(1.5, 2)),
  lcc_band_samples_per_band = 500L,
  lcc_band_max_attempts = 1000L,
  lcc_band_time_budget_s = 900,
  lcc_band_stall_attempts = 150L,
  lcc_discovery_max_unique = 50000L,
  lcc_library_max_size = 5000L,
  bfs_reservation_lcc = 500L,
  sec_size_bands = list(c(5, 8), c(8, 12), c(12, 20)),
  library_density_threshold = 15,
  tree_sec_n_trees = 200L,
  bfs_sec_quota_per_band = 25L,
  sec_library_max_size = 500L,
  bfs_reservation_sec = 100L
)
}
\arguments{
\item{discovery_capacity_multiplier}{Discovery-only capacity bound
(multiplier of min_capacity); LCCs above this are skipped during tree
enumeration purely for speed, not because they're infeasible}

\item{tree_lcc_n_trees}{Number of spanning trees to sample for LCC
tree-cut enumeration}

\item{bfs_lcc_n_samples, bfs_lcc_n_seeds}{BFS-supplement sample/seed counts
for LCC discovery}

\item{lcc_capacity_bands_relative}{List of \code{c(low, high)} capacity bands
(relative to min_capacity) for capacity-stratified BFS discovery}

\item{lcc_band_samples_per_band}{Target number of LCC samples per capacity
band}

\item{lcc_band_max_attempts}{Maximum BFS attempts per band before giving up}

\item{lcc_band_time_budget_s}{Wall-clock budget per band (seconds)}

\item{lcc_band_stall_attempts}{Consecutive attempts without a valid
candidate before a discovery pass gives up on a band}

\item{lcc_discovery_max_unique}{Cap on unique LCCs tree enumeration will
discover before it stops sampling more trees}

\item{lcc_library_max_size}{Final LCC library size cap}

\item{bfs_reservation_lcc}{Reserved LCC-library slots for BFS discoveries}

\item{sec_size_bands}{List of \code{c(min_acres, max_acres)} bands for secondary
block discovery}

\item{library_density_threshold}{Minimum density (units/acre) for library
blocks}

\item{tree_sec_n_trees}{Number of spanning trees to sample for secondary
tree-cut enumeration}

\item{bfs_sec_quota_per_band}{BFS-supplement quota per secondary size band}

\item{sec_library_max_size}{Final secondary library size cap}

\item{bfs_reservation_sec}{Reserved secondary-library slots for BFS
discoveries}
}
\value{
Named list of discovery tuning parameters
}
\description{
Bundles the tuning parameters for building the LCC and secondary block
libraries: tree enumeration (Wilson's algorithm) plus a BFS supplement,
for each library. Defaults mirror the values previously hardcoded in
\code{inst/targets/temp_targets_parcel_config.R}.
}

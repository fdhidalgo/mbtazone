% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_parcel_mcmc_state.R
\name{get_or_build_lcc_state}
\alias{get_or_build_lcc_state}
\title{Look up or build (and cache) the secondary-free LCC-only state for a library LCC}
\usage{
get_or_build_lcc_state(
  lcc_id,
  lcc_parcels,
  library,
  parcel_graph,
  neighbor_idx = NULL,
  parcel_names = NULL,
  nbr_from = NULL,
  nbr_to = NULL,
  cache = NULL
)
}
\arguments{
\item{lcc_id}{Integer library block id of the LCC.}

\item{lcc_parcels}{Character vector of the LCC's parcel ids.}

\item{library}{Secondary library.}

\item{parcel_graph}{igraph object.}

\item{neighbor_idx, parcel_names, nbr_from, nbr_to}{Passed through to
\code{reset_to_lcc()} on a cache miss.}

\item{cache}{An environment used as the id -> state store (or NULL to disable).}
}
\value{
The LCC-only state (cached object on a hit).
}
\description{
The LCC-only state produced by \code{reset_to_lcc()} for a library LCC is a
pure function of (LCC parcel set, secondary library, graph) and therefore
invariant across MCMC proposals. Library block ids are monotonic and never
reused (eviction only NULLs slots), so caching keyed by \code{lcc_id} is safe.
This near-eliminates the dominant \code{reset_to_lcc} cost on the replace-LCC
path for revisited LCCs.
}
\keyword{internal}

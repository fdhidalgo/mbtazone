% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_config.R
\name{parcel_target_spec}
\alias{parcel_target_spec}
\title{Define the parcel MCMC target spec}
\usage{
parcel_target_spec(
  district_data,
  parcel_graph_result,
  capacity_prior_lambda,
  k_prior_lambda,
  right_of_way_sf = NULL,
  row_fill_m = 50
)
}
\arguments{
\item{district_data}{List containing district_requirements from data loading}

\item{capacity_prior_lambda}{Linear capacity-prior strength (penalizes
capacity above min_capacity; larger values favor configurations closer
to the minimum)}

\item{k_prior_lambda}{Geometric-prior rate on the number of secondary
blocks (k); 0 is a flat/improper prior over k}

\item{right_of_way_sf}{Optional sf object of right-of-way polygons passed
through to \code{\link[=define_constraints]{define_constraints()}} for ROW-constrained fill. Typically
\code{district_data$district_right_of_way}.}

\item{row_fill_m}{Passed through to \code{\link[=define_constraints]{define_constraints()}}.}
}
\value{
List with \code{constraints} and \code{priors} sub-lists
}
\description{
Composes the hard district constraints with the priors that shape the
sampled distribution. Constraints and priors are kept as separate
sub-fields rather than merged into one flat list: constraints gate
feasibility (hard, checked in \code{check_hard_constraints_only()}), while
priors reweight among feasible states (soft, applied via the MH ratio).
This is the single object that determines the target distribution
pi(state) — nothing about proposal/kernel tuning belongs here.
}

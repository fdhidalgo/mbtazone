% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_bfs_utils.R
\name{bfs_grow_block}
\alias{bfs_grow_block}
\title{Grow a connected block via randomized BFS}
\usage{
bfs_grow_block(
  graph,
  metric_lookup,
  seed_pool = NULL,
  eligible_pool = NULL,
  target_min,
  target_max = Inf,
  target_exact = NULL,
  check_max_before_add = FALSE
)
}
\arguments{
\item{graph}{igraph object with named vertices}

\item{metric_lookup}{Named numeric vector (area or capacity per node)}

\item{seed_pool}{Character vector of eligible seed nodes (NULL = all vertices)}

\item{eligible_pool}{Character vector of nodes eligible for expansion (NULL = all vertices)}

\item{target_min}{Minimum metric threshold to reach}

\item{target_max}{Maximum metric threshold (optional, for pre-add rejection)}

\item{target_exact}{Optional exact target to grow toward (overrides target_min for termination)}

\item{check_max_before_add}{Logical: if TRUE, reject additions that would exceed target_max}
}
\value{
List with:
\itemize{
\item block: character vector of node IDs in grown block
\item metric_total: final metric sum
\item success: logical (TRUE if target_min reached)
\item seed: the seed node used
}
}
\description{
Generic BFS region-growing that supports various termination conditions
and frontier filters. Consolidates multiple BFS patterns used across
the codebase for block/component generation.
}
\keyword{internal}

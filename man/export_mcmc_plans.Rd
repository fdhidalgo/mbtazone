% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_export.R
\name{export_mcmc_plans}
\alias{export_mcmc_plans}
\title{Export thinned MCMC samples as a wide parcel-plan indicator table}
\usage{
export_mcmc_plans(
  chain_results,
  parcel_graph_result,
  secondary_library,
  district_name,
  burn_in = 0L,
  n_samples = Inf,
  seed = 42L
)
}
\arguments{
\item{chain_results}{Named list from all_parcel_chain_results}

\item{parcel_graph_result}{Result from build_parcel_graph_target()}

\item{secondary_library}{Hydrated secondary library}

\item{district_name}{Character district name for ID construction}

\item{burn_in}{Steps to treat as burn-in and exclude (default 0). Samples
whose step index is <= burn_in are dropped before selection.}

\item{n_samples}{Number of post-burn-in plans to export. Use Inf (default)
to export all post-burn-in samples.}

\item{seed}{Random seed, used only when n_samples < Inf (default 42)}
}
\value{
data.table with columns:
parcel_id, district, inclusion_frequency, plan_0001, plan_0002, ...
where plan columns are 1 if parcel is in that plan, 0 otherwise
}
\description{
Produces a wide-format data.table with one row per parcel and one binary
indicator column per sampled plan. Suitable for joining to parcel-level
data and computing inclusion frequencies directly.
}

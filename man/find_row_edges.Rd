% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_graph_building.R
\name{find_row_edges}
\alias{find_row_edges}
\title{Find cross-ROW edges for a set of candidate parcel pairs}
\usage{
find_row_edges(
  parcels_sf,
  candidate_from,
  candidate_to,
  row_union,
  max_dist_m,
  min_coverage_ratio = 0.9,
  row_gap_tolerance_m = 1,
  verbose = TRUE
)
}
\arguments{
\item{parcels_sf}{sf with LOC_ID and geometry for ALL district parcels}

\item{candidate_from}{integer indices into parcels_sf for "from" parcels}

\item{candidate_to}{integer indices into parcels_sf for "to" parcels}

\item{row_union}{Pre-computed unioned ROW sfc}

\item{max_dist_m}{Hard cap on nearest-points line length in metres}

\item{min_coverage_ratio}{Minimum ROW coverage fraction (default 0.9)}

\item{row_gap_tolerance_m}{Tolerance for parcel-to-ROW gap (data quality) (default 1m)}

\item{verbose}{Print progress messages}
}
\value{
logical vector (length == nrow candidates), TRUE for valid ROW edges
}
\description{
Given candidate pairs (already filtered to non-touching), validates them as
genuine cross-ROW connections using nearest-points lines and a ROW coverage
ratio. Parcels not within row_gap_tolerance_m of the ROW are excluded early
as they cannot participate in cross-ROW connections.
}
